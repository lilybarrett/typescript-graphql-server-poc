# import Channel from "../types/channel/schema.graphql"

type Query {
    channels: [Channel]
    # channel(id: Int): Channel
    channelByName(name: String): Channel
}

type Mutation {
    createChannel(name: String): Channel
    updateChannel(id: Int): Channel
}

